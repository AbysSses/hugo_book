# Hugo Book 知识库 - 快速开始

## 📋 目录

1. [前置准备](#前置准备)
2. [在 Obsidian 中创建发布目录](#在-obsidian-中创建发布目录)
3. [首次部署](#首次部署)
4. [日常使用](#日常使用)
5. [故障排除](#故障排除)

---

## 前置准备

### ✅ 确认已安装

- [x] Hugo v0.153.0+ (extended 版本)
- [x] Git
- [x] Obsidian
- [x] terminal-notifier (可选，用于系统通知)

### 检查环境

```bash
# 检查 Hugo 版本
hugo version
# 应显示：hugo v0.153.0+extended

# 检查 Git
git --version

# 检查当前项目
cd /Users/hcyang/AI/gemini_project/hugo_site/my_book
ls -la
```

---

## 在 Obsidian 中创建发布目录

### 步骤 1：创建目录结构

在 Obsidian vault 中创建以下结构：

```
LifeOS/
└── 6. Website/
    └── 2. hugo_book/          ← 新建这个目录
        ├── _index.md           ← 首页
        ├── getting-started/    ← 示例章节（文档）
        │   ├── _index.md
        │   └── installation.md
        ├── reference/          ← 参考资料（文档）
        │   ├── _index.md
        │   └── api.md
        └── posts/              ← 博客文章（可选）
            ├── _index.md
            └── my-first-post.md
```

### 步骤 2：创建首页文件

在 Obsidian 中创建文件：
**路径**: `6. Website/2. hugo_book/_index.md`

```markdown
---
title: "知识库首页"
type: docs
---

# 欢迎来到我的知识库

这是基于 Hugo Book 主题构建的个人知识库。

## 目录

- [快速开始](getting-started/)
- [参考资料](reference/)
```

### 步骤 3：创建示例章节

**文件 1**: `getting-started/_index.md`
```markdown
---
title: "快速开始"
weight: 1
bookCollapseSection: false
---

# 快速开始

这里是入门指南。
```

**文件 2**: `getting-started/installation.md`
```markdown
---
title: "安装指南"
weight: 1
bookToc: true
---

# 安装指南

这是一个示例页面。

## 前置条件

列出需要的软件...

## 安装步骤

1. 第一步
2. 第二步
```

### 步骤 4：配置 Obsidian 附件目录（可选）

在 Obsidian 设置中：
1. 打开 **设置 → 文件与链接**
2. 设置 **附件默认存放路径** 为：`6. Website/attachments`
3. 这样所有图片会统一存放，便于同步到 Hugo

---

## 首次部署

### 步骤 1：测试本地预览

```bash
cd /Users/hcyang/AI/gemini_project/hugo_site/my_book

# 启动 Hugo 开发服务器
hugo server -D

# 访问 http://localhost:1313
# 检查网站是否正常显示
```

### 步骤 2：首次同步测试

```bash
# 执行部署脚本（会同步 Obsidian 内容）
./deploy.sh
```

**脚本会执行：**
1. 📚 从 Obsidian 同步内容到 `content/docs/`
2. 🎨 检查主题是否有修改
3. 📦 提交并推送到 GitHub
4. 🔔 发送系统通知（如果安装了 terminal-notifier）

### 步骤 3：等待 Vercel 部署

- 推送到 GitHub 后，Vercel 会自动检测
- 约 1-2 分钟后访问：https://book.hcyang.us.kg/
- 检查内容是否正确显示

---

## 日常使用

### 场景 1：写文章并发布

**流程：**

1. **在 Obsidian 中写作**
   - 打开 Obsidian
   - 在 `6. Website/2. hugo_book/` 目录下创建/编辑文件
   - 使用 Markdown 语法写作
   - 添加 Front Matter（见下方示例）

2. **本地预览（可选）**
   ```bash
   cd ~/AI/gemini_project/hugo_site/my_book
   hugo server -D
   # 访问 http://localhost:1313 查看效果
   ```

3. **发布到网站**
   ```bash
   ./deploy.sh
   ```

4. **等待部署完成**
   - 约 1-2 分钟后刷新网站

### 场景 2：快速推送配置修改

如果只是修改了 `hugo.toml`、主题样式等，不涉及 Obsidian 内容：

```bash
./push.sh "修改网站配置"
```

### 场景 3：添加图片

1. **在 Obsidian 中插入图片**
   - 图片会自动保存到 `6. Website/attachments/` 目录
   - 使用标准 Markdown 语法：`![描述](/attachments/image.png)`

2. **部署**
   ```bash
   ./deploy.sh
   # 脚本会自动同步附件目录
   ```

---

## Front Matter 示例

### 普通页面

```markdown
---
title: "页面标题"
weight: 1              # 菜单排序（数字越小越靠前）
bookToc: true          # 显示右侧目录
bookHidden: false      # 是否在菜单中隐藏
---

# 页面标题

正文内容...
```

### 章节页（_index.md）

```markdown
---
title: "第一章：基础知识"
weight: 1
bookCollapseSection: false  # 是否默认折叠
bookFlatSection: false       # 是否平铺显示子页面
---

# 第一章：基础知识

章节介绍...
```

### 博客文章（如果使用 posts/）

```markdown
---
title: "文章标题"
date: 2024-12-22
tags: ["标签1", "标签2"]
---

博客内容...
```

---

## 项目结构说明

### Hugo 项目目录

```
my_book/
├── content/
│   └── docs/              ← 同步自 Obsidian
│       ├── _index.md
│       ├── getting-started/
│       └── reference/
├── static/
│   └── attachments/       ← 同步自 Obsidian 附件
├── themes/
│   └── hugo-book/         ← Git submodule
├── hugo.toml              ← 网站配置
├── vercel.json            ← Vercel 部署配置
├── deploy.sh              ← 完整部署脚本 ⭐
├── push.sh                ← 快速推送脚本
├── README.md              ← Hugo Book 使用指南
├── plan.md                ← 技术方案文档
└── SETUP.md               ← 本文件
```

### Obsidian 目录

```
LifeOS/
└── 6. Website/
    ├── 1. hugo_siyu/      ← 博客网站（已有）
    ├── 2. hugo_book/      ← 知识库网站（新建）⭐
    │   ├── _index.md
    │   ├── getting-started/  ← 文档内容
    │   ├── reference/        ← 文档内容
    │   └── posts/            ← 博客文章（可选）
    │       ├── _index.md
    │       └── *.md
    └── attachments/       ← 统一的图片/附件目录
```

---

## 脚本使用说明

### deploy.sh - 完整部署脚本

**用途**：
- 同步 Obsidian 内容到 Hugo
- 处理主题 submodule
- 提交并推送到 GitHub
- 触发 Vercel 自动部署

**使用**：
```bash
./deploy.sh
```

**执行流程**：
```
1. 检查 Obsidian 源目录
   ↓
2. rsync 同步内容到 content/docs/
   ↓
3. rsync 同步附件到 static/attachments/
   ↓
4. 检查并提交主题修改（如果有）
   ↓
5. git add . && git commit
   ↓
6. git push origin main
   ↓
7. 发送系统通知
```

**输出示例**：
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 同步 Obsidian 知识库到 Hugo...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  源目录: /Users/.../hugo_book
  目标目录: .../content/docs
✅ 内容同步完成
  → 同步附件...
  ✓ 附件同步完成

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎨 检查主题修改...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  → 主题无变化

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 提交到 GitHub...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ 已推送到 GitHub

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 发布完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 网站地址: https://book.hcyang.us.kg/
⏱️  Vercel 构建大约需要 1-2 分钟
```

### push.sh - 快速推送脚本

**用途**：
- 不同步 Obsidian
- 仅推送 Hugo 项目本身的修改
- 适合修改配置、主题样式等

**使用**：
```bash
# 使用默认提交信息
./push.sh

# 使用自定义提交信息
./push.sh "修改网站标题"
```

---

## 配置修改

### 修改网站标题

编辑 `hugo.toml`:
```toml
title = '你的知识库名称'
```

### 修改域名

编辑 `hugo.toml`:
```toml
baseURL = 'https://your-domain.com/'
```

### 调整主题颜色

创建 `assets/_custom.scss`:
```scss
:root {
  --body-background: #ffffff;
  --body-font-color: #333333;
}
```

### 关闭搜索功能

编辑 `hugo.toml`:
```toml
[params]
  BookSearch = false
```

---

## 故障排除

### 问题 1：deploy.sh 提示找不到 Obsidian 目录

**错误信息**：
```
⚠️  警告：Obsidian 源目录不存在
   路径：.../hugo_book
   跳过内容同步，仅推送本地修改...
```

**解决方案**：
1. 检查 Obsidian 中是否已创建 `6. Website/2. hugo_book/` 目录
2. 确认路径是否正确（检查 deploy.sh 中的 `OBSIDIAN_BOOK` 变量）
3. 如果路径不同，修改 deploy.sh 中的配置

### 问题 2：内容同步了但网站没更新

**可能原因**：
1. Vercel 还在构建中（等待 1-2 分钟）
2. 浏览器缓存（强制刷新：Cmd+Shift+R）
3. Git 推送失败（检查终端输出）

**检查步骤**：
```bash
# 1. 检查 Git 状态
git status

# 2. 检查最近的提交
git log --oneline -3

# 3. 检查 Vercel 部署日志
# 访问 https://vercel.com/dashboard
```

### 问题 3：图片不显示

**检查清单**：
- [ ] 图片是否在 `static/attachments/` 目录
- [ ] Markdown 中使用的是绝对路径：`/attachments/image.png`
- [ ] 文件名是否包含中文或特殊字符
- [ ] deploy.sh 是否成功同步了附件目录

**解决方案**：
```bash
# 手动检查附件目录
ls -la static/attachments/

# 手动同步附件（如果需要）
rsync -av \
  "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/LifeOS/6. Website/attachments/" \
  static/attachments/
```

### 问题 4：菜单不显示内容

**检查清单**：
- [ ] 内容是否在 `content/docs/` 目录
- [ ] 每个文件夹是否有 `_index.md`
- [ ] Front Matter 中是否设置了 `title`
- [ ] 是否设置了 `bookHidden: true`

**解决方案**：
1. 确保每个目录都有 `_index.md`
2. 检查 Front Matter 格式是否正确
3. 清除浏览器缓存后重试

### 问题 5：主题样式异常

**解决方案**：
```bash
# 重新初始化主题 submodule
git submodule update --init --recursive

# 检查主题状态
cd themes/hugo-book
git status
git log --oneline -3
```

---

## 常用命令速查

### 本地开发

```bash
# 启动开发服务器
hugo server -D

# 启动服务器（仅显示已发布内容）
hugo server

# 构建网站到 public/ 目录
hugo --minify
```

### Git 操作

```bash
# 查看状态
git status

# 查看最近提交
git log --oneline -5

# 查看远程仓库
git remote -v

# 强制刷新（慎用）
git fetch --all
git reset --hard origin/main
```

### 主题管理

```bash
# 更新主题到最新版本
cd themes/hugo-book
git pull origin main
cd ../..
git add themes/hugo-book
git commit -m "Update hugo-book theme"
```

---

## 下一步

完成设置后，你可以：

1. ✅ 在 Obsidian 中创建更多章节和页面
2. ✅ 自定义主题颜色和样式
3. ✅ 添加数学公式（使用 KaTeX）
4. ✅ 添加 Mermaid 图表
5. ✅ 探索 Hugo Book 的更多功能

### 学习资源

- **Hugo Book 文档**: https://github.com/alex-shpak/hugo-book
- **Hugo 官方文档**: https://gohugo.io/documentation/
- **Markdown 指南**: https://www.markdownguide.org/
- **本项目文档**:
  - `README.md` - Hugo Book 详细使用指南
  - `plan.md` - 技术方案和架构设计

---

## 获取帮助

如果遇到问题：

1. 查看本文档的故障排除部分
2. 阅读 `README.md` 了解 Hugo Book 的详细功能
3. 查看 `plan.md` 了解技术实现细节
4. 检查 Vercel 部署日志
5. 查阅 Hugo Book GitHub Issues

---

**祝你使用愉快！** 🎉
