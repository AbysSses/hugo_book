---
tags:
  - linux
  - tutorial
---

```bash
## 临时修改
export HTTP_PROXY="http://127.0.0.1:7897" ### 这里的端口要看使用的VPN到底用的哪个端口
export HTTPS_PROXY="http://127.0.0.1:7897"

```

需要完全修改的话把上面命令加入 `~/.zshrc` 或 `~/.bash_profile`