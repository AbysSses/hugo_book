# Hugo Book 知识库项目

基于 Hugo Book 主题的个人知识库，类似书籍的文档结构，支持多层级目录、搜索、深色模式等功能。

## 项目信息

- **网站地址**: https://book.hcyang.us.kg/
- **Hugo 版本**: v0.153.0+
- **主题**: [Hugo Book](https://github.com/alex-shpak/hugo-book)
- **托管平台**: Vercel
- **主题特点**:
  - 清爽的书籍式布局
  - 自动生成左侧菜单树
  - 内置搜索功能
  - 支持深色模式
  - 数学公式 (KaTeX)
  - Mermaid 图表
  - 多语言支持

## Hugo Book 标准内容结构

### 1. 核心目录结构

```
my_book/
├── content/
│   ├── docs/                    # 主要文档内容（自动生成菜单）
│   │   ├── _index.md           # 章节首页
│   │   ├── chapter-1/          # 第一章
│   │   │   ├── _index.md       # 章节说明
│   │   │   ├── section-1.md    # 小节 1
│   │   │   └── section-2.md    # 小节 2
│   │   ├── chapter-2/          # 第二章
│   │   │   ├── _index.md
│   │   │   ├── subsection/     # 支持多层嵌套
│   │   │   │   ├── _index.md
│   │   │   │   └── page.md
│   │   │   └── another-page.md
│   │   └── reference/          # 参考资料
│   │       └── api.md
│   │
│   ├── posts/                   # 博客文章（可选）
│   │   ├── _index.md
│   │   └── my-post.md
│   │
│   └── _index.md               # 网站首页
│
├── static/                      # 静态资源
│   ├── images/
│   └── files/
│
├── hugo.toml                    # 配置文件
└── README.md                    # 项目说明
```

### 2. 内容组织原则

#### 📁 `docs/` 目录（核心文档）

**Hugo Book 的核心功能区域**，所有放在 `docs/` 目录下的内容会：
- 自动出现在左侧导航菜单
- 按照 `weight` 参数排序
- 支持无限层级嵌套

**组织规则：**
```
content/docs/
├── _index.md           # 必需，章节根页面
├── 01-intro/           # 使用数字前缀控制顺序（可选）
│   ├── _index.md       # 章节说明页
│   └── basic.md        # 具体内容页
└── 02-advanced/
    ├── _index.md
    └── deep-dive.md
```

**重要文件：`_index.md`**
- 每个文件夹都应该有一个 `_index.md`
- 作用：定义该章节的标题、权重、是否折叠等
- 示例：
```markdown
---
title: "第一章：基础知识"
weight: 1
bookCollapseSection: false  # 是否默认折叠子章节
bookFlatSection: false       # 是否平铺显示（不显示层级）
---

# 第一章：基础知识

这是第一章的介绍内容...
```

#### 📝 普通页面（如 `basic.md`）

**Front Matter 参数：**
```markdown
---
title: "页面标题"
weight: 1                    # 排序权重，数字越小越靠前
bookToc: true                # 是否显示右侧目录
bookHidden: false            # 是否在菜单中隐藏
bookCollapseSection: false   # 如果有子页面，是否折叠
bookComments: false          # 是否启用评论
bookSearchExclude: false     # 是否从搜索中排除
---

# 页面内容

这里是 Markdown 内容...
```

**菜单显示逻辑：**
1. 默认按文件名字母顺序排列
2. 使用 `weight` 参数精确控制顺序
3. 使用 `title` 参数自定义菜单显示名称
4. 使用 `bookHidden: true` 隐藏不想在菜单显示的页面

#### 📰 `posts/` 目录（博客功能，可选）

如果需要时间线式的博客内容：
```
content/posts/
├── _index.md
├── 2024-12-01-post-1.md
└── 2024-12-15-post-2.md
```

**博客页面 Front Matter：**
```markdown
---
title: "博客标题"
date: 2024-12-22
tags: ["标签1", "标签2"]
categories: ["分类"]
---

博客内容...
```

### 3. 层级结构示例

**示例：技术文档知识库**

```
content/docs/
├── _index.md                          # 文档首页
├── getting-started/                   # 第 1 部分：入门指南
│   ├── _index.md                      # weight: 1
│   ├── installation.md                # weight: 1
│   ├── quick-start.md                 # weight: 2
│   └── configuration.md               # weight: 3
├── programming/                       # 第 2 部分：编程语言
│   ├── _index.md                      # weight: 2
│   ├── python/                        # 2.1 Python
│   │   ├── _index.md
│   │   ├── basics.md
│   │   └── advanced.md
│   ├── javascript/                    # 2.2 JavaScript
│   │   ├── _index.md
│   │   └── async.md
│   └── go/                            # 2.3 Go
│       └── _index.md
├── database/                          # 第 3 部分：数据库
│   ├── _index.md                      # weight: 3
│   ├── sql/
│   │   ├── _index.md
│   │   └── basics.md
│   └── nosql/
│       └── _index.md
└── reference/                         # 第 4 部分：参考资料
    ├── _index.md                      # weight: 99
    ├── api.md
    └── glossary.md
```

**渲染后的菜单效果：**
```
📚 文档首页
├── 📖 入门指南
│   ├── 安装
│   ├── 快速开始
│   └── 配置
├── 💻 编程语言
│   ├── 📘 Python
│   │   ├── 基础
│   │   └── 进阶
│   ├── 📙 JavaScript
│   │   └── 异步编程
│   └── 📗 Go
├── 🗄️ 数据库
│   ├── SQL
│   │   └── 基础
│   └── NoSQL
└── 📑 参考资料
    ├── API 文档
    └── 术语表
```

### 4. Front Matter 参数详解

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `title` | string | 文件名 | 页面标题（显示在菜单和页面顶部） |
| `weight` | int | 0 | 排序权重（越小越靠前） |
| `bookFlatSection` | bool | false | 平铺显示子页面（不显示层级结构） |
| `bookToc` | bool | true | 是否显示右侧目录（TOC） |
| `bookHidden` | bool | false | 是否在菜单中隐藏此页面 |
| `bookCollapseSection` | bool | false | 默认折叠该章节的子页面 |
| `bookComments` | bool | false | 是否启用评论功能 |
| `bookSearchExclude` | bool | false | 是否从搜索结果中排除 |
| `bookHref` | string | - | 自定义链接（用于外部链接） |
| `bookIcon` | string | - | 自定义菜单图标 |

### 5. 文件命名建议

**推荐的命名方式：**

1. **使用数字前缀控制顺序（推荐）**
   ```
   01-introduction.md
   02-getting-started.md
   03-advanced-topics.md
   ```
   - 优点：直观，文件系统中顺序与菜单一致
   - 缺点：重新排序时需要重命名文件

2. **使用 weight 参数控制顺序**
   ```
   introduction.md    # weight: 1
   getting-started.md # weight: 2
   advanced-topics.md # weight: 3
   ```
   - 优点：灵活，不需要重命名文件
   - 缺点：需要在每个文件中设置 weight

3. **混合方式（最推荐）**
   ```
   01-basics/
   ├── _index.md           # weight: 1
   ├── intro.md            # weight: 1
   └── setup.md            # weight: 2
   02-advanced/
   └── _index.md           # weight: 2
   ```
   - 文件夹用数字前缀
   - 文件用 weight 参数

### 6. 特殊功能

#### 链接到外部 URL

在 `_index.md` 中使用 `bookHref`：
```markdown
---
title: "官方文档"
bookHref: "https://gohugo.io/documentation/"
---
```

点击菜单项会跳转到外部链接。

#### 隐藏页面但保留访问

```markdown
---
title: "草稿页面"
bookHidden: true
---
```

页面不会出现在菜单中，但可以通过直接 URL 访问。

#### 平铺章节（不显示层级）

```markdown
---
title: "API 参考"
bookFlatSection: true
---
```

所有子页面会平铺显示，不显示父子关系。

## 当前配置

### hugo.toml 配置说明

```toml
baseURL = 'https://book.hcyang.us.kg/'
languageCode = 'zh-cn'
title = '我的知识库'
theme = 'hugo-book'

[params]
  BookSearch = true        # 开启搜索功能
  BookTheme = 'auto'       # 主题模式：light/dark/auto
  BookMenuBundle = '/menu' # 菜单生成方式（推荐使用文件树）
  BookPortableLinks = true # 便携式链接（相对路径）

  [params.katex]
    enable = true          # 启用数学公式

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true        # 允许 HTML/JS（Mermaid 必需）

  [markup.tableOfContents]
    startLevel = 2         # TOC 起始级别
    endLevel = 4           # TOC 结束级别
```

## 本地开发

### 启动开发服务器

```bash
cd /Users/hcyang/AI/gemini_project/hugo_site/my_book
hugo server -D

# 访问 http://localhost:1313
```

### 创建新页面

```bash
# 创建文档页面
hugo new docs/chapter-1/new-page.md

# 创建博客文章
hugo new posts/my-new-post.md
```

### 手动构建

```bash
hugo --minify
# 生成到 public/ 目录
```

## 部署说明

### Vercel 部署

项目通过 Git 推送自动部署到 Vercel：

```bash
git add .
git commit -m "更新内容"
git push origin main
```

Vercel 会自动：
1. 检测推送
2. 初始化 Git submodules（主题）
3. 使用 Hugo v0.153.0 构建
4. 部署到 https://book.hcyang.us.kg/

### vercel.json 配置

```json
{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "build": {
    "env": {
      "HUGO_VERSION": "0.153.0"
    }
  },
  "installCommand": "git submodule update --init --recursive && echo 'Submodules initialized'"
}
```

## 内容同步（计划中）

详见 `plan.md` 文件，计划实现：
- 从 Obsidian 自动同步内容
- 使用软连接或部署脚本
- 保持 Obsidian 和 Hugo 内容一致

## 常用操作

### 调整页面顺序

编辑页面的 `weight` 参数：
```markdown
---
title: "页面标题"
weight: 5  # 修改这个值
---
```

### 折叠/展开章节

编辑章节 `_index.md`：
```markdown
---
title: "章节标题"
bookCollapseSection: true  # 默认折叠
---
```

### 添加图片

1. 将图片放到 `static/images/` 目录
2. 在 Markdown 中引用：
```markdown
![图片说明](/images/screenshot.png)
```

### 添加数学公式

```markdown
行内公式：$E = mc^2$

块级公式：
$$
\int_{a}^{b} f(x) dx
$$
```

### 添加 Mermaid 图表

```markdown
{{</* mermaid */>}}
graph TD
    A[开始] --> B[处理]
    B --> C[结束]
{{</* /mermaid */>}}
```

## 主题自定义

### 自定义样式

创建 `assets/_custom.scss`：
```scss
// 自定义颜色
:root {
  --body-background: #ffffff;
  --body-font-color: #333333;
}

// 自定义字体
body {
  font-family: "PingFang SC", "Microsoft YaHei", sans-serif;
}
```

### 自定义布局

在 `layouts/` 目录覆盖主题模板：
```
layouts/
├── _default/
│   └── baseof.html
└── partials/
    └── docs/
        └── menu.html
```

## 故障排除

### 菜单不显示内容

检查：
1. 内容是否在 `content/docs/` 目录
2. 是否有 `_index.md` 文件
3. Front matter 中是否设置了 `bookHidden: true`

### 搜索功能不工作

确保 `hugo.toml` 中：
```toml
[params]
  BookSearch = true
```

### 数学公式不显示

确保启用 KaTeX：
```toml
[params.katex]
  enable = true
```

### Vercel 构建失败

检查：
1. `vercel.json` 中的 Hugo 版本
2. Git submodule 是否正确初始化
3. Vercel 构建日志中的错误信息

## 参考资源

- Hugo Book 主题文档: https://github.com/alex-shpak/hugo-book
- Hugo 官方文档: https://gohugo.io/documentation/
- Markdown 语法: https://www.markdownguide.org/
- KaTeX 数学公式: https://katex.org/
- Mermaid 图表: https://mermaid.js.org/

## 项目结构总结

```
Hugo Book 内容组织模式：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📂 content/docs/           ← 主文档区（自动生成菜单）
   ├── 使用 _index.md 定义章节
   ├── 使用 weight 控制顺序
   ├── 支持无限层级嵌套
   └── 每个文件夹都应有 _index.md

📂 content/posts/          ← 博客区（可选）
   └── 时间线式文章

📂 static/                 ← 静态资源
   ├── images/
   └── files/

📄 hugo.toml              ← 站点配置
   ├── BookSearch: 搜索
   ├── BookTheme: 深色模式
   └── katex: 数学公式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
